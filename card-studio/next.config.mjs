/** @type {import('next').NextConfig} */
const nextConfig = {
  // Card art + content live under ./deck and are read/written at runtime by
  // the API routes. Nothing special needed here for the local studio.
};

export default nextConfig;
