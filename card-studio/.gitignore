# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — keys live here, never commit them
.env
.env*.local

# typescript
*.tsbuildinfo

# NOTE: /deck is intentionally NOT ignored.
# Card content, prompts, and generated images are committed so you can
# `git commit` the sets you like and diff/restore between generations.
